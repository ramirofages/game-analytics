<%= form_for @checkpoint, url: {action: action} do |f| %>
  <% if @checkpoint.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@checkpoint.errors.count, "error") %> prohibited this checkpoint from being saved:</h2>

      <ul>
      <% @checkpoint.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :level_id %><br>
    <%= f.text_field :level_id %>
  </div>

  <div class="field">
    <%= f.label :activationCount %><br>
    <%= f.text_field :activationCount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
